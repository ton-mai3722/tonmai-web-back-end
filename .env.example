# Port configuration (Default: 5000)
# Note for macOS: If port 5000 is occupied by AirPlay Receiver (ControlCenter),
# you can either turn off "AirPlay Receiver" in System Settings > AirDrop & AirPlay,
# or change PORT to 5001.
PORT=5000
